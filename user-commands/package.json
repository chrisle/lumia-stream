{
  "name": "lumia-plugin-user_commands",
  "version": "1.0.0",
  "private": true,
  "description": "Allow privileged viewers to create user chat commands",
  "main": "build/main.js",
  "scripts": {
    "clean": "node scripts/clean.js --all",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "package": "node scripts/package.js",
    "update-manifest": "node scripts/update_manifest.js"
  },
  "dependencies": {
    "@lumiastream/plugin": "^0.2.9"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "terser": "^5.46.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.3.0"
  }
}
